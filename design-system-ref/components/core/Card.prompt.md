Elevated dark surface panel. Hairline border lights glacial-blue with a lift + glow on hover when `interactive`.

```jsx
<Card interactive padding="var(--sp-6)">
  <h3>Signaux IA</h3>
  <p>Lecture du carnet d'ordres en temps réel.</p>
</Card>
```

Props: `interactive` (hover lift + accent border), `glow` (resting soft glow), `padding`.
